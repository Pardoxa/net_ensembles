initSidebarItems({"fn":[["merged_log10_prob","Merge probability density of multiple rees simulationsWill calculate the merged log (base 10) probability density. Also returns the corresponding histogram.If an interval has multiple walkers, their probability will be merged before all probabilities are aligned`rees` does not need to be sorted in any wayErrorswill return `HistErrors::EmptySlice` if the `rees` slice is emptywill return other HistErrors if the intervals have no overlap"],["merged_log10_probability_and_align","Merge probability density of multiple rees simulationssame as merged_log_probability_and_align but all logarithms are now base 10"],["merged_log_prob","Merge probability density of multiple rees simulationsWill calculate the merged log (base e) probability density. Also returns the corresponding histogram.`rees` does not need to be sorted in any wayErrorswill return `HistErrors::EmptySlice` if the `rees` slice is emptywill return other HistErrors if the intervals have no overlap"],["merged_log_probability_and_align","Merge probability density of multiple rees simulationsWill calculate the merged log (base e) probability density (second return parameter). also returns the corresponding histogram (first return parameter)also returns aligned probability densities of intervals (base e) (third return parameter). They will be NaN for all entries, which the corresponding interval did not coverIf an interval has multiple walkers, their probability will be merged before all probabilities are aligned`rees` does not need to be sorted in any wayErrorswill return `HistErrors::EmptySlice` if the `rees` slice is emptywill return other HistErrors if the intervals have no overlap"]],"struct":[["ReesWalker","Walker for Replica exchange entropic samplingperformes the random walk in its respective domain "],["ReplicaExchangeEntropicSampling",""]],"type":[["Rees",""]]});